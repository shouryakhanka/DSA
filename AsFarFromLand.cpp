#include <iostream>
#include <vector>

using namespace std;

// a multi source breadth search problem, we should solve this prob.
// in a opposite way, worst case run time of bfs is O(n ^ 2), and we
// have n ^ 2 in this grid, so it would become O(n ^ 4)

int maxDistance(vector<vector<int>> &grid)
{

    int main()
    {
        return 0;
    }